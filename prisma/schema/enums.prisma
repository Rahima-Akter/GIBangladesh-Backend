enum Role {
  USER
  ADMIN
  SUPER_ADMIN
}

enum ProductStatus {
  ACTIVE
  INACTIVE
}

enum BlogStatus {
  PUBLISHED
  DRAFT
}

enum AiFeatureType {
  CONTENT_GENERATOR
  TEXT_REWRITER
  CHAT_ASSISTANT
  HASHTAG_GENERATOR
}

enum AnalyticsEventType {
  PRODUCT_VIEW
  BLOG_VIEW
  PRODUCT_LIKE
  BLOG_LIKE
  COMMENT_CREATED
  AI_USED
}
